"""PathoIntelligence: AI-assisted histopathology cancer detection platform."""

__version__ = "0.1.0"


def main() -> None:
    """CLI entrypoint (placeholder). Real service is run via uvicorn — see api/main.py."""
    print("Run the API with: uv run uvicorn pathointelligence.api.main:app --reload")
